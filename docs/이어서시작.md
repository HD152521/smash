# 이어서 시작

## 지금 상태

**Phase 0~10 구현 완료.** 배포 전 남은 일은 아래 "남은 일" 뿐입니다.

```
검증        단위 91 · 실DB 경기 18 · 실DB 보안 19 · 스키마 11
명령        npm run verify   (typecheck + lint + test + CSP + build)
개발 서버    npm run dev
```

## 화면 구조

```
/login                      이메일 로그인 (소셜은 켜면 자동 노출)
/                           메인: 참가 · 생성 · 내 대회
/join                       초대 코드 6자리
/new                        대회 만들기 (조 개수 · 조커 개수)
/my                         내 대회 모음

/t/:id                      대회 메인 — 코트별 현황만 (실시간)
   /setup                   조 선택 온보딩 (한 번만)
   /settings                내 조 변경
   /referee                 내가 심판인 경기 → 눌러서 채점
   /standings               조별 순위
   /live                    관전 화면 (TV·태블릿)
   /admin                   관리 — 상태 · 코트 · 멤버 · 초대코드
      /matches/new          경기 편성 / 지난 결과 입력
      /audit                변경 기록
   /matches/:matchId        심판 채점 화면
```

원칙: **화면 하나가 기능 하나.** 대회 메인에 다 얹으면 정작 필요한 점수가 밀립니다.

## 도메인 규칙

```
조 개수 N, 조커조 K  →  1조~K조가 조커조로 고정
목표 점수   일반 21점 / 조커 11점   (듀스 없음)
승점        일반 승리 1.0 / 조커 승리 0.5 / 패배 0
경기        조 vs 조, 각 조에서 관리자가 2명씩 (복식)
코트        한 코트에 진행 중 경기는 하나 (부분 유니크 인덱스로 DB 가 강제)
심판        관리자가 아니라 편성 때 지정된 참가자
```

## 절대 깨면 안 되는 것

1. **점수는 원장 + 투영**
   `score_events` 에 append, `matches.score_a/b` 로 투영.
   `score_events` 에 쓰기 RLS 정책이 하나도 없는 건 의도입니다 — RPC 우회를 막습니다.
   `matches` 의 점수·상태·승자 컬럼도 트리거로 직접 수정을 막습니다.

2. **목표점수·승점은 `match_teams` 스냅샷**
   대회 도중 조커 구성을 바꿔도 지난 결과와 순위가 소급 변조되지 않습니다.

3. **`rules.ts` 와 `record_score` 는 같은 판정**
   화면용 사본입니다. 한쪽만 고치면 화면과 DB 가 어긋납니다.

4. **대기 큐의 진실은 ref 하나**
   `useMatchScoring` 에서 localStorage 는 거울, state 는 렌더용입니다.
   셋을 섞어 읽으면 저장 실패 환경(사파리 시크릿)에서 큐가 쪼그라듭니다.

5. **낙관적 점수를 따로 세지 않는다**
   `pending` 에서 파생합니다. 별도 카운터를 두면 실시간 갱신과 경쟁해 이중 계산됩니다.

## 남은 일

### 배포 전 필수
1. **Supabase → Authentication → Providers → Email → `Confirm email` OFF**
   내장 메일은 시간당 몇 통이라 가입을 몇 번만 시도해도 막힙니다.
   실서비스 전에 Resend/SES 붙이고 다시 켜세요.
2. **Vercel 연결** — GitHub import → 환경변수 2개(`VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`)
   → 도메인 `smash.juganlab.com` 연결.
   `SUPABASE_SECRET_KEY` · `SUPABASE_DB_URL` 은 **넣지 마세요** (로컬 전용).
3. **실제 폰 2대로 리허설** — 와이파이를 껐다 켜면서 채점.
   대기 큐가 실제로 밀리는지, 화면이 안 꺼지는지.

### 선택
4. 소셜 로그인 (구글·카카오) — 코드는 이미 준비됨. 대시보드에서 켜면 버튼이 자동으로 나타납니다.
   redirect URI 는 전부 `https://vwocyjuwormeaentdost.supabase.co/auth/v1/callback`
   (앱 도메인이 아닙니다)
5. `eslint-plugin-jsx-a11y` 도입
6. `MatchCreatePage.tsx` 분할 (464줄)
7. 순위 동률 시 승자승은 클라이언트에서 계산 중 — 조가 많아지면 SQL 로 옮길 것

## 유용한 스크립트

```bash
npm run verify              전체 검증
npm run db:push             마이그레이션 적용
npm run db:types            스키마에서 타입 생성 (Docker 불필요)
npm run db:verify           RLS·권한·Realtime 구조 검증 11항목
npm run db:smoke            인증·권한 실DB 검증 17항목
npm run db:smoke:security   공격 시나리오 19건
npm run db:smoke:match      경기 한 판 전체 흐름 18항목
npm run db:seed             데모 대회에 참가자 16명 배정
npm run demo:live           데모 진행 중 경기 생성 / score A 로 득점
npm run db:confirm          미인증 계정 일괄 확인
```

## 이 프로젝트에서 배운 함정

- **`interface` 로 뽑은 Row 는 `Record<string, unknown>` 에 할당되지 않는다.**
  supabase-js 스키마 인식이 통째로 실패하고 `.rpc()` 가 조용히 `any` 가 됨.
  `database.typetest.ts` 가 지킵니다.
- **`raise exception` 은 같은 트랜잭션의 기록도 되돌린다.**
  브루트포스 차단 카운터가 영원히 0 이었습니다. → 예외 대신 결과 반환.
- **RLS 는 행 단위라 컬럼을 못 가린다.**
  admin 이 `owner_id` 를 바꿔 대회를 탈취할 수 있었습니다. → BEFORE 트리거.
- **JSX 에서 `배열.length && ...` 는 길이가 0 일 때 `0` 을 출력한다.**
- **async 본문이 동기적으로 끝나면 `finally` 가 변수 할당보다 먼저 돈다.**
- **뷰 컬럼은 Postgres 가 NOT NULL 을 보존하지 않는다** → 생성 타입이 전부 nullable.
